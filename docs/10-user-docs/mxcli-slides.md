# mxcli: AI-Powered Development for Mendix

## Slide Deck for Presentation

---

## Slide 1: Title

# mxcli
## AI-Powered Development for Mendix

**Accelerate Mendix Development with Natural Language and Code Generation**

---

## Slide 2: The Challenge

### Traditional Mendix Development

- Visual modeling is powerful but **click-intensive**
- AI tools struggle with **binary/JSON model formats**
- Limited ability to **batch modify** multiple elements
- Difficult to **review changes** in pull requests
- **Context limitations** when using GenAI APIs

---

## Slide 3: The Solution

### Introducing mxcli

A command-line interface that brings **AI-powered development** to Mendix

- **MDL** (Mendix Definition Language) - SQL-like syntax for models
- **Read & write** Mendix projects programmatically
- **Native integration** with AI coding assistants
- Works with **local .mpr files** and **Studio Pro**

---

## Slide 4: Key Benefits

### Why mxcli?

| Benefit | Impact |
|---------|--------|
| **10x Faster Development** | Natural language to working features |
| **Token Efficient** | 5-10x fewer tokens than JSON |
| **Better Code Reviews** | Human-readable diffs |
| **Consistent Standards** | MDL templates enforce best practices |
| **Full Project Understanding** | Agentic search capabilities |

---

## Slide 5: How It Works

### Architecture

```
┌─────────────────┐     ┌──────────────┐     ┌─────────────────┐
│   Developer     │     │    mxcli     │     │ Mendix project  │
│   or AI agent   │────▶│  (MDL REPL)  │────▶│   (.mpr file)   │
│                 │ MDL │              │     │                 │
│ "create a       │     │ Parses MDL   │     │ Creates actual  │
│  Customer       │     │ Validates    │     │ entities,       │
│  entity..."     │     │ Executes     │     │ microflows,     │
└─────────────────┘     └──────────────┘     │ pages, etc.     │
                                             └─────────────────┘
```

---

## Slide 6: The MDL Advantage

### Compared to JSON Model Representations

- **Generated by AI** - LLMs produce syntactically correct MDL
- **Human-Readable Diffs** - Easier to review than binary/JSON
- **High Information Density** - See complete microflow logic at a glance
- **Token Efficient** - 5-10x fewer tokens than JSON
- **Concise Skills** - Better AI instruction files = better output

---

## Slide 7: Token Efficiency

### Why This Matters for GenAI

| Representation | Tokens (10-entity module) |
|----------------|---------------------------|
| JSON (raw model) | ~15,000-25,000 tokens |
| MDL | ~2,000-4,000 tokens |
| **Savings** | **5-10x fewer tokens** |

**Benefits:**
- Lower API costs
- More context in prompts
- Larger scope operations
- Better AI understanding

---

## Slide 8: Token Comparison

### JSON vs MDL

**JSON (~180 tokens):**
```json
{
  "entity": {
    "name": "Customer",
    "attributes": [
      {"name": "Name", "type": {"type": "string", "length": 200}},
      {"name": "Email", "type": {"type": "string", "length": 200}}
    ]
  }
}
```

**MDL (~35 tokens):**
```sql
/** Customer master data */
create persistent entity Sales.Customer (
    Name: string(200),
    Email: string(200)
);
```

---

## Slide 9: MDL Example - Entities

### Domain Model Definition

```sql
/** Order entity with audit support */
@position(100, 100)
create persistent entity Sales.Order (
    OrderNumber: string(50) not null unique,
    OrderDate: datetime default '[%CurrentDateTime%]',
    TotalAmount: decimal not null default 0,
    status: enumeration(Sales.OrderStatus) default 'Draft'
);

create association Sales.Order_Customer
    from Sales.Order to Sales.Customer
    type reference
    delete_behavior DELETE_BUT_KEEP_REFERENCES;
```

---

## Slide 10: MDL Example - Microflows

### Business Logic

```sql
/**
 * Process a new order submission.
 * Validates, updates inventory, sends confirmation.
 */
create microflow Sales.SubmitOrder($order: Sales.Order)
returns boolean as $success
begin
    if not call Sales.ValidateOrder($order) then
        return false;
    end;

    change $order (status = 'Submitted');
    commit $order;
    call Sales.SendOrderConfirmation($order);
    return true;
end;
```

---

## Slide 11: MDL Example - Pages

### User Interface

```sql
create page Sales.CustomerOverview
title 'Customer Management'
layout 'Atlas_Core.Atlas_Default'
begin
  layoutgrid mainGrid
    row
      column (DESKTOP_WIDTH 12)
        datagrid CustomerGrid
          source database Sales.Customer
          sort by Name asc
        begin
          header
            actionbutton btnNew 'New' action create_object;
          column Name as 'Customer Name';
          column Email as 'Email';
        end;
end;
```

---

## Slide 12: AI Integration

### Claude Code Workflow

```
your-mendix-project/
├── .claude/
│   └── skills/           # AI generation guides
│       ├── create-entity.md
│       ├── create-microflow.md
│       └── create-page.md
├── CLAUDE.md             # project context
└── mxcli                 # CLI executable
```

**Developer says:** "Create a customer management module"

**AI generates:** Complete MDL with entities, microflows, and pages

---

## Slide 13: AI Generation Skills

### Combining Capabilities

| Skill | Output |
|-------|--------|
| Domain Model | Entities, associations, enumerations |
| Overview Pages | Data grids with CRUD buttons |
| Detail Pages | Edit forms with validation |
| Validation Microflows | Business rule enforcement |

**Skills combine automatically:**
- "Create Product catalog" → Domain Model + Pages + Validation

---

## Slide 14: Agentic Search

### AI-Powered Code Understanding

```sql
/** Query project metadata */
select Name, EntityType, AttributeCount
from CATALOG.ENTITIES
where ModuleName = 'Sales';

/** Find duplicate entities */
select e1.Name, e2.Name, count(*) as MatchingAttrs
from CATALOG.ATTRIBUTES a1
join CATALOG.ATTRIBUTES a2 on a1.Name = a2.Name
where a1.EntityId != a2.EntityId
GROUP by e1.Name, e2.Name
having count(*) >= 3;
```

---

## Slide 15: Quality Analysis

### Built-in Code Quality Queries

- **Duplicate Detection** - Find similar entities across modules
- **Documentation Coverage** - Identify undocumented elements
- **Complexity Analysis** - Find entities needing refactoring
- **Orphan Detection** - Entities with no associations
- **Naming Conventions** - Validate PascalCase compliance
- **Security Audit** - Check access rule coverage

---

## Slide 16: Best Practices as Code

### MDL Templates Enforce Standards

```sql
/**
 * Template: Standard Audit Fields.
 * All entities should include these fields.
 */
@position(${X}, ${Y})
create persistent entity ${module}.${EntityName} (
    ${BusinessFields}

    /** Standard audit fields */
    CreatedAt: datetime default '[%CurrentDateTime%]',
    CreatedBy: string(200),
    ModifiedAt: datetime,
    ModifiedBy: string(200),
    IsDeleted: boolean default false
);
```

---

## Slide 17: Now Available

### Recently Shipped Capabilities

| Feature | Description |
|---------|-------------|
| **VS Code Extension + LSP** | Syntax highlighting, autocomplete, hover, go-to-definition |
| **External SQL** | Query PostgreSQL, Oracle, SQL Server directly |
| **Data Import** | Import from external databases into Mendix app DB |
| **Connector Generation** | Auto-generate Database Connector MDL from schema |
| **Linting & Reports** | 40+ rules, scored best practices reports |
| **Testing Framework** | `.test.mdl` / `.test.md` test files |

---

## Slide 18: Future Vision

### Planned Capabilities

| Feature | Description |
|---------|-------------|
| **REST Clients** | Define external API integrations |
| **OData Publishing** | Expose entities as OData services |
| **Workflow Definition** | Business process automation |

---

## Slide 19: Summary

### mxcli Transforms Mendix Development

| Capability | Benefit |
|------------|---------|
| **MDL Language** | Human and AI-readable definitions |
| **GenAI Integration** | Natural language to features |
| **Token Efficiency** | 5-10x cheaper, more context |
| **Template System** | Enforce organizational standards |
| **Agentic Search** | AI-powered project understanding |

---

## Slide 20: Getting Started

### Quick Start

```bash
# Navigate to your Mendix project
cd /path/to/your-mendix-project

# Initialize mxcli
./mxcli init

# Start the REPL
./mxcli

# connect and explore
mendix> connect local 'MyProject.mpr';
mendix> show modules;
mendix> describe entity Sales.Customer;
```

---

## Slide 21: Call to Action

### Next Steps

1. **Try mxcli** in a development project
2. **Explore MDL syntax** with existing models
3. **Integrate with Claude Code** for AI-assisted development
4. **Define templates** for your organization's standards
5. **Provide feedback** to shape future development

---

## Slide 22: Questions?

# Thank You

**Resources:**
- Documentation: `docs/`
- MDL Syntax Reference: `docs/02-features/mdl/`
- Example Projects: `templates/`

---
